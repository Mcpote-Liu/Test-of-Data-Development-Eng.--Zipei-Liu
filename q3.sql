SELECT COUNT(DISTINCT user_id) AS user_count
FROM event_log
WHERE event_timestamp BETWEEN UNIX_TIMESTAMP('2020-09-01 00:00:00') AND UNIX_TIMESTAMP('2020-09-30 23:59:59') --用 UNIX_TIMESTAMP() 转成 epoch 秒数
GROUP BY user_id
HAVING COUNT(*) >= 1000 AND COUNT(*) < 2000;
